{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "postinstall": "cd ../client && npm install && npm run build", 
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file .env server.js",
    "dev": "nodemon --env-file .env server.js ",
    "client": "cd ../client && npm run dev",
    "dev:all": "concurrently --kill-others \"npm run dev\" \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  }
}
